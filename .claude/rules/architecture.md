---
paths:
  - "pgsrip/**"
---

# Project map

`docs/architecture.md` has one line for each module in `pgsrip/`. When you add, remove, rename, or change
the role of a module, update that line in the same commit.
